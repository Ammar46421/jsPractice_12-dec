// method 1

// const divide = (a, b)=> a/b;

// const multiply = (a, b)=> a*b;

// const subtract = (a, b)=> a-b;

// const add = (a, b)=> a+b;

// export default add;

// export {
//     divide,
//     multiply,
//     subtract,
// }


// method 2

const divide = (a, b)=> a/b;

const multiply = (a, b)=> a*b;

const subtract = (a, b)=> a-b;

const add = (a, b)=> a+b;

export default add;

export {
    divide,
    multiply,
    subtract,
    add,
}